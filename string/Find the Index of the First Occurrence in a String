class Solution {
public:
    int strStr(string haystack, string needle) {
        // string built in function and kmp algo 2 pointer method
        int i = haystack.find(needle);
        if(i != string::npos){
            return i;
        }
        else return -1;
    }
};
